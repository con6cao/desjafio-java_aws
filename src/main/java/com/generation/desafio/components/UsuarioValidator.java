package com.generation.desafio.components;

import org.springframework.stereotype.Component;

@Component
public class UsuarioValidator {

/*
    public void validarDuplicata(Long id, UsuarioRepository usuarioRepository ) {
        if (!(usuarioRepository.findById(id) ==null)){
            throw new UsuarioDuplicateException("Usuario faz parte da insituição");
        }

    }

    public void validarNome(String nome) {
        String expression = "[A-Z]";
        Pattern pattern = Pattern.compile(expression, Pattern.CASE_INSENSITIVE);
        Matcher matcher = pattern.matcher(nome);
        if (!matcher.matches()) {
            throw new FormatoInvalidoException("Formato Invalido de Nome");
        }
    }

 */
}
